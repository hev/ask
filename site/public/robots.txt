# hevask.com — agents welcome. Emphatically: this site is a tool for
# making docs agent-readable, so blocking agents would be self-refuting.
#
# Policy is fully open: crawl it, index it, ground an answer in it, train
# on it. Three ways in, cheapest first:
#
#   /llms.txt        orientation — what this is, what to read
#   /llms-full.txt   the whole corpus in one fetch
#   /api/ask         the digest itself — tree, cat, grep over the docs
#                    (see /docs/api/cli; keyless for keyword reads)
#
# The named groups are redundant against the wildcard on purpose. A more
# specific user-agent group beats `*`, so if a platform default ever
# re-injects a `Disallow: /` for one of these, the explicit Allow still
# stands. Cloudflare's AI-crawler block did exactly that to hevmind.com.

User-agent: *
Content-Signal: search=yes, ai-input=yes, ai-train=yes
Allow: /

# Assistants fetching because a person asked, the indexers behind AI
# search, and the training crawlers — all three, all allowed.
User-agent: ChatGPT-User
User-agent: Claude-User
User-agent: Perplexity-User
User-agent: OAI-SearchBot
User-agent: Claude-SearchBot
User-agent: PerplexityBot
User-agent: GPTBot
User-agent: ClaudeBot
User-agent: Google-Extended
User-agent: Applebot-Extended
User-agent: CCBot
User-agent: meta-externalagent
User-agent: Amazonbot
User-agent: Bytespider
Allow: /

Sitemap: https://hevask.com/sitemap-index.xml
